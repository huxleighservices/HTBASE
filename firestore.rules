
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // These rules are wide open for development.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
