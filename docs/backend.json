{
  "entities": {
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents a user within the HTBase.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the User entity."
        },
        "firstName": {
          "type": "string",
          "description": "First name of the user."
        },
        "lastName": {
          "type": "string",
          "description": "Last name of the user."
        },
        "email": {
          "type": "string",
          "description": "Email address of the user.",
          "format": "email"
        }
      },
      "required": [
        "id",
        "email"
      ]
    },
    "Client": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Client",
      "type": "object",
      "description": "Represents a client of the user.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Client entity."
        },
        "displayId": {
          "type": "string",
          "description": "A random 6-character ID for display purposes."
        },
        "firmName": {
          "type": "string"
        },
        "legalFirstName": {
          "type": "string"
        },
        "legalLastName": {
          "type": "string"
        },
        "firmSize": {
          "type": "string"
        },
        "firmEstYear": {
          "type": "string"
        },
        "industry": {
          "type": "string"
        },
        "contactEmail": {
          "type": "string",
          "format": "email"
        },
        "contactPhoneNumber": {
          "type": "string"
        },
        "location": {
          "type": "string"
        },
        "status": {
          "type": "string",
          "enum": ["pending", "active", "archived"]
        },
        "launchPassword": {
          "type": "string",
          "description": "Password to access the client's launch portal."
        },
        "trainingData": {
          "type": "string",
          "description": "Custom product information or training material for the AI."
        }
      },
      "required": [
        "id",
        "displayId",
        "firmName",
        "contactEmail",
        "status"
      ]
    },
    "Session": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Session",
      "type": "object",
      "description": "Represents a training session for a client.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Session entity."
        },
        "firstName": {
          "type": "string"
        },
        "lastName": {
          "type": "string"
        },
        "email": {
          "type": "string",
          "format": "email"
        },
        "companyName": {
          "type": "string"
        }
      },
      "required": [
        "id",
        "firstName",
        "lastName",
        "email",
        "companyName"
      ]
    },
    "TrainingSession": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "TrainingSession",
      "description": "Represents a user's training session, containing multiple results.",
      "type": "object",
      "properties": {
        "id": {
          "type": "string"
        },
        "name": {
          "type": "string"
        },
        "createdAt": {
          "type": "string",
          "format": "date-time"
        },
        "results": {
          "type": "array",
          "items": {
            "type": "object"
          }
        }
      },
      "required": [
        "id",
        "name",
        "createdAt"
      ]
    },
    "BrandCustomization": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "BrandCustomization",
      "type": "object",
      "description": "Stores the brand customization settings for a client's launch portal.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Should always be 'config'"
        },
        "primaryColor": {
          "type": "string",
          "description": "Primary color in HSL format (e.g., '262.1 83.3% 57.8%')."
        },
        "backgroundColor": {
          "type": "string",
          "description": "Background color in HSL format."
        },
        "accentColor": {
          "type": "string",
          "description": "Accent color in HSL format."
        },
        "logoUrl": {
          "type": "string",
          "format": "uri",
          "description": "URL for the client's logo."
        },
        "tagline": {
          "type": "string",
          "description": "Custom tagline or name for the portal."
        }
      },
      "required": ["id"]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "User",
          "schema": {
            "$ref": "#/entities/User"
          },
          "description": "Collection to store user profiles. Only the authenticated user can read/write their own profile.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the user."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/clients/{clientId}",
        "definition": {
          "entityName": "Client",
          "schema": {
            "$ref": "#/entities/Client"
          },
          "description": "Subcollection to store clients for a specific user.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the user who owns the client."
            },
            {
              "name": "clientId",
              "description": "The unique identifier of the client."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/clients/{clientId}/sessions/{sessionId}",
        "definition": {
          "entityName": "TrainingSession",
          "schema": {
            "$ref": "#/entities/TrainingSession"
          },
          "description": "Subcollection to store training sessions for a specific client.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the user who owns the client."
            },
            {
              "name": "clientId",
              "description": "The unique identifier of the client."
            },
            {
              "name": "sessionId",
              "description": "The unique identifier of the session."
            }
          ]
        }
      },
       {
        "path": "/users/{userId}/clients/{clientId}/customization/{customizationId}",
        "definition": {
          "entityName": "BrandCustomization",
          "schema": { "$ref": "#/entities/BrandCustomization" },
          "description": "Stores brand customization for a specific client portal. The document ID should be a singleton, like 'config'.",
          "params": [
            { "name": "userId", "description": "The user owner." },
            { "name": "clientId", "description": "The client being customized." },
            { "name": "customizationId", "description": "Singleton ID, e.g., 'config'." }
          ]
        }
      }
    ],
    "reasoning": "The Firestore structure is designed to be secure, scalable, and easily debuggable, following the core design principles and strategy mandates. It uses path-based ownership for user-specific data, ensuring clear authorization and efficient querying. The structure avoids hierarchical authorization dependencies by relying solely on `request.auth.uid` and path-based ownership, which simplifies security rules and allows atomic operations. Segregation of data based on access needs simplifies security rules by ensuring all documents in a collection share the same security requirements.\n\nThe `/users/{userId}` collection stores user profiles. The `/users/{userId}/landingPages/{landingPageId}` collection stores landing pages owned by each user. The `/users/{userId}/customizations/{customizationId}` collection stores customizations owned by each user. This structure facilitates straightforward security rules based on path-based ownership, guaranteeing that only the authenticated user can access their respective landing pages and customizations. All `list` operations are secured via path-based ownership, where listing a collection is only allowed if the authenticated user is the owner. \n\nThis design emphasizes Authorization Independence, DBAC (Database-Based Access Control), QAPs (Rules Are Not Filters), and structural segregation, resulting in a robust and maintainable data model."
  }
}
